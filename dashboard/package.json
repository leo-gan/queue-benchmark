{
  "name": "queue-benchmark-dashboard",
  "version": "0.1.0",
  "description": "Interactive analytics dashboard for queue benchmarks",
  "scripts": {
    "dev": "vite --port 5173 --open",
    "build": "vite build",
    "preview": "vite preview",
    "sync-data": "python3 scripts/sync-data.py",
    "sync-experiments": "python3 scripts/sync-experiments.py",
    "test": "node --test test/exp-graphs.test.js"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "type": "module"
}
