# JavaScript Queue Benchmark

`Array` (baseline), `denque` / `yocto-queue` (O(1) FIFO), `fastq` (job queue),
`p-queue` (scheduler — not a handoff queue).

```bash
cd javascript
./scripts/run-benchmarks.sh smoke
```
